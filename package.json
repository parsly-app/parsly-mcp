{
  "name": "parsly-mcp",
  "version": "0.0.1",
  "description": "Real browser automation for AI agents. Parsly connects Claude, Cursor, and ChatGPT to Chrome via MCP — read structured data from any site you can open in a tab.",
  "license": "MIT",
  "homepage": "https://parsly.to",
  "repository": {
    "type": "git",
    "url": "https://github.com/parsly-app/parsly-mcp"
  },
  "keywords": [
    "mcp",
    "browser-automation",
    "web-automation",
    "chrome-extension",
    "instagram",
    "reddit",
    "twitter",
    "data-reading"
  ],
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "bin": {
    "parsly-mcp": "./dist/bin/parsly-mcp.js",
    "parsly-mcp-http": "./dist/bin/parsly-mcp-http.js"
  },
  "main": "./dist/src/index.js",
  "exports": {
    ".": "./dist/src/index.js"
  },
  "files": [
    "dist",
    "dist-bundle",
    "manifest.json",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "tsc",
    "bundle": "esbuild bin/parsly-mcp.ts --bundle --platform=node --format=cjs --outfile=dist-bundle/server.cjs",
    "pack": "npm run bundle && mcpb pack && mv mcp.mcpb parsly-mcp.mcpb",
    "start": "node dist/bin/parsly-mcp-http.js",
    "start:stdio": "node dist/bin/parsly-mcp.js",
    "check-types": "tsc --noEmit",
    "version:patch": "npm version patch --no-git-tag-version",
    "version:minor": "npm version minor --no-git-tag-version",
    "version:major": "npm version major --no-git-tag-version"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.18.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "latest",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "esbuild": "latest",
    "tsx": "^4.21.0",
    "typescript": "5.9.2"
  }
}
